"""Figure layer boundary.

Concrete plots are intentionally deferred until the confirmatory analysis and
visual encodings are prespecified. Figure code must consume saved statistics,
never invoke models or silently recompute generations.
"""

